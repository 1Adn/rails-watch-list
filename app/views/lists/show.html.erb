<h1>Content of the list : <%= @list.name %></h1>
<ul>
  <% @list.bookmarks.each do |bookmark| %>
    <li>
      <h3><%= bookmark.movie.title%></h3>
      <%= link_to bookmark_path(bookmark), data: { turbo_method: :delete, turbo_confirm: "Are you sure you want to delete that Task?" } do %>
        <i class="btn btn fa-regular fa-trash-can" style="color: #C4503F;"></i>
      <% end %>
      <p><%= bookmark.comment %>
        <p>
        </li>
      <% end %>
    </ul>
    <div><%= link_to "Add bookmark", new_list_bookmark_path(@list) %>
      <div><%= link_to "<- Back", lists_path %> </div>
